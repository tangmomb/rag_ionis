# SQL Beautifier

Petite interface web locale pour formatter une requête SQL inline.

Ouvrir `index.html` dans un navigateur, ou servir ce dossier avec un serveur HTTP local. L’outil ne se connecte à aucune base de données et ne transmet pas le contenu de la requête.
